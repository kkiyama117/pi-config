---
description: Create a git commit from current changes
---
Review all staged and unstaged changes in this git repository.
Run `git diff --cached` and `git diff` to see all changes.
Then stage all changes with `git add -A` and create a git commit with a
concise conventional commit message.
Do not modify any files other than staging them for commit.
